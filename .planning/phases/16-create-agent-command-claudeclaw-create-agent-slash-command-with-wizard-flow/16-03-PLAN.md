---
phase: 16-create-agent-command
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - skills/create-agent/SKILL.md
  - src/__tests__/agents.test.ts
autonomous: false

requirements: [PHASE-16]

must_haves:
  truths:
    - "skills/create-agent/SKILL.md is auto-discovered by src/skills.ts"
    - "Wizard asks the 6 questions from issue #78 one at a time"
    - "Wizard validates name and schedule before scaffolding"
    - "End-to-end integration test creates an agent and verifies all files exist with expected content"
    - "Manual wizard run successfully scaffolds a test agent (Suzy v2) in under 2 minutes"
  artifacts:
    - skills/create-agent/SKILL.md
  key_links:
    - "Wizard calls createAgent() from src/agents.ts via Write tool or bun shell"
    - "Skill auto-discovered through existing skills.ts collectSkillsFromDir"
---

<objective>
Ship the `/claudeclaw:create-agent` wizard skill and lock in a full integration test. Final human checkpoint verifies the wizard experience matches the create-skill pattern's conversational vibe.

Purpose: Deliver the actual user-facing wizard — the phase's headline feature.
Output: skills/create-agent/SKILL.md + integration test + manual verification.
</objective>

<context>
@.planning/phases/16-create-agent-command-claudeclaw-create-agent-slash-command-with-wizard-flow/16-CONTEXT.md
@.planning/phases/16-create-agent-command-claudeclaw-create-agent-slash-command-with-wizard-flow/16-RESEARCH.md
@skills/create-skill/SKILL.md
@src/skills.ts
@src/agents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/create-agent/SKILL.md wizard</name>
  <files>skills/create-agent/SKILL.md</files>
  <action>
    Mirror the structure of skills/create-skill/SKILL.md. YAML frontmatter:
    ```yaml
    ---
    name: create-agent
    description: Use when the user wants to create a new agent teammate, scaffold a scheduled Claude persona, or asks to "create an agent", "new agent", or "/claudeclaw:create-agent".
    ---
    ```
    Body: conversational wizard instructions that tell Claude to:
    1. Ask for **name** (kebab-case). Validate immediately by calling `validateAgentName` via a short `bun -e` snippet or by eyeballing + checking `agents/&lt;name&gt;` doesn't exist.
    2. Ask for **role** (one line: what does this agent do?).
    3. Ask for **personality** (free text, 2-4 sentences).
    4. Ask for **schedule** (natural language like "every weekday at 9am", raw cron, or "none"). Validate with `parseScheduleToCron` before proceeding.
    5. Ask for **discord channels** (comma-separated list or "none" — metadata only for Phase 16).
    6. Ask for **data sources** (free text — what info sources does it use?).
    Then call `createAgent()` from src/agents.ts. Preferred pattern: a short `bun -e "import {createAgent} from './src/agents'; await createAgent({...})"` invocation via the Bash tool, passing all wizard inputs. On success, print a summary with the scaffolded paths and next steps (how to invoke via `claudeclaw send --agent &lt;name&gt;`, mention job file if schedule provided).

    Tone: friendly, one question per message, matches create-skill's vibe. Include explicit "DO NOT dump all questions at once" instruction.
  </action>
  <verify>
    <automated>bun -e "import {listSkills} from './src/skills'; const s = await listSkills(); if (!s.some(x =&gt; x.name === 'create-agent')) process.exit(1)"</automated>
  </verify>
  <done>Skill file exists, auto-discovered by skills.ts, frontmatter valid, wizard body contains all 6 steps with validation instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Add end-to-end integration test</name>
  <files>src/__tests__/agents.test.ts</files>
  <action>
    Append a new describe block `"integration: full agent lifecycle"` that:
    1. Calls createAgent({ name: "test-suzy", role: "...", personality: "...", schedule: "daily at 9am", discordChannels: ["#test"], dataSources: "RSS feeds" })
    2. Asserts agents/test-suzy/ contains all 4 .md files + .gitignore
    3. Asserts IDENTITY.md contains the role string
    4. Asserts SOUL.md contains the personality string
    5. Asserts CLAUDE.md contains "#test" and "RSS feeds"
    6. Asserts .claude/claudeclaw/jobs/test-suzy.md exists with `schedule: 0 9 * * *` and `agent: test-suzy`
    7. Asserts listAgents() includes "test-suzy"
    8. Asserts loadAgent("test-suzy") returns correct paths
    9. Cleanup in afterEach.
  </action>
  <verify>
    <automated>bun test src/__tests__/agents.test.ts</automated>
  </verify>
  <done>Integration test passes alongside unit tests from plan 01.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Manual wizard walkthrough</name>
  <files>(manual)</files>
  <action>
    Run the wizard interactively:
    1. Invoke `/claudeclaw:create-agent` (or trigger the skill however claudeclaw surfaces skills)
    2. Answer the 6 questions with the target test values ("suzy-v2", "daily content sourcer", "sharp and curious, loves weird internet corners", "every weekday at 9am", "#content", "RSS + HackerNews + Lobsters")
    3. Verify: wizard asks one question at a time, validation catches a bad name (try "Suzy" first), schedule parses correctly, scaffold completes in under 2 minutes
    4. After scaffold: inspect agents/suzy-v2/ directory, open each file, confirm content matches inputs
    5. Run `claudeclaw send --agent suzy-v2 "say hello"` and verify agent responds with its personality
    6. Delete test agent dir + job file after verification
  </action>
  <verify>User confirms: wizard UX matches create-skill vibe, scaffolding works, ad-hoc send routes to agent session.</verify>
  <done>User signs off on the wizard experience; Suzy v2 test agent successfully created and invoked; cleanup complete.</done>
</task>

</tasks>

<verification>
- `bun test` — full suite green including new integration test
- Skill auto-discovery check passes
- Human checkpoint signed off
</verification>

<success_criteria>
A user can create a new agent teammate end-to-end via the wizard in under 2 minutes, the agent persists across invocations, and scheduled jobs route to the agent's session. Phase 16 deliverable complete.
</success_criteria>

<output>
Create `.planning/phases/16-create-agent-command-claudeclaw-create-agent-slash-command-with-wizard-flow/16-03-SUMMARY.md` after completion.
</output>
