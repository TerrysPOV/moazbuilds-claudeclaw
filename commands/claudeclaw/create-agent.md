---
description: Create a new ClaudeClaw agent (identity, workflow, personality, scheduled jobs)
argument-hint: [agent-name]
---

Use the `create-agent` skill to walk the user through creating a new agent named "$ARGUMENTS".

If $ARGUMENTS is empty, ask for the agent name first.
